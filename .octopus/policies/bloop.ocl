name = "Bloop"
violation_action = "block"

conditions {
    rego = <<-EOT
            package bloop
            
            default result := {"allowed": true}
            EOT
}

scope {
    rego = <<-EOT
            package bloop
            
            default evaluate := true
            EOT
}